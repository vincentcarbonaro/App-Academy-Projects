
<h1>All Cats</h1>


<ul>
  <% @cats.each do |cat| %>
  <li>
    <a href="<%= cat_url(cat) %>"><%= cat.name %></a>
  </li>
  <% end %>
</ul>

<% if current_user.nil? %>
  <a href="<%= new_session_url %>">Log In</a>
<% else %>
  Welcome, <%= current_user.user_name %>.
<% end %>
